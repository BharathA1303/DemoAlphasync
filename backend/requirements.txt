fastapi==0.109.2
uvicorn[standard]==0.27.1
sqlalchemy==2.0.25
alembic==1.13.1
pydantic==2.6.1
pydantic-settings==2.1.0
python-multipart==0.0.9
aiohttp==3.9.3
websockets>=13.1,<16.0
httpx>=0.25.0,<1.0
python-dotenv==1.0.1
email-validator==2.1.0.post1
asyncpg==0.29.0
aiosqlite>=0.20.0,<1.0
psycopg2-binary>=2.9.9,<3.0
requests>=2.31.0,<3.0
numpy>=1.26.0,<3.0
pytz>=2024.1,<2025.0

# ── Local Authentication (JWT + password hashing) ──────────────
# passlib's bcrypt backend is incompatible with bcrypt>=4.1 (passlib
# reads bcrypt.__about__.__version__, removed upstream) and misbehaves
# with a bogus "password cannot be longer than 72 bytes" error on any
# password. Use the bcrypt library directly instead.
bcrypt>=4.0.0,<5.0
python-jose[cryptography]>=3.3.0,<4.0

# ── Market Provider & Cache ─────────────────────────────────────
redis[asyncio]>=5.0.0,<6.0

# ── Data Feed Secret Encryption ────────────────────────────────
cryptography>=42.0.0,<44.0

# ── Admin Panel ──────────────────────────────────────────────────
aiosmtplib>=2.0.0,<4.0
Jinja2>=3.1.0,<4.0
openpyxl>=3.1.0,<4.0

# ── Production WSGI/ASGI server ────────────────────────────────
gunicorn>=21.2.0,<23.0
