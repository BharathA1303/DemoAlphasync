# Development/CI-only tooling for backend/app/ (the new clean-architecture
# rewrite). Not installed in the production image.
-r requirements.txt

mypy>=1.10.0,<2.0
ruff>=0.5.0,<1.0
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0
pytest-cov>=5.0.0,<6.0
